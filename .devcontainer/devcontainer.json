{
  "name": "Flutter Codespace",
  "image": "mcr.microsoft.com/vscode/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt update && sudo apt install -y curl git unzip xz-utils zip && git clone https://github.com/flutter/flutter.git -b stable ~/flutter && echo 'export PATH=\"$PATH:$HOME/flutter/bin\"' >> ~/.bashrc && source ~/.bashrc && flutter doctor",
  "forwardPorts": [8080],
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ]
    }
  }
}
